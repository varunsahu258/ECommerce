{
  "name": "@ecommerce/platform-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:api": "vitest run --dir api",
    "test:selenium": "python3 -m pip install -r selenium/requirements.txt && python3 selenium/smoke_scrape.py"
  },
  "dependencies": {
    "supertest": "^7.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/supertest": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
